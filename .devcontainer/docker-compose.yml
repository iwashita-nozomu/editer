services:
  workspace:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    working_dir: /workspace
    volumes:
      - ..:/workspace:cached
    command: /bin/bash -lc "sleep infinity"
    tty: true
    init: true
