# Project-scoped Codex settings.
# Keep review fan-out explicit, but do not fix agent depth in repo config.

approval_policy = "never"
sandbox_mode = "danger-full-access"

[agents]
max_threads = 8
job_max_runtime_seconds = 1800
