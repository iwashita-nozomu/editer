---
name: worktree-start
description: Use this skill when creating, recreating, or resuming a worktree and you need to fix scope, references, action log paths, and kickoff checks before editing.
---

# Worktree Start

1. Read `agents/skills/worktree-start.md`.
1. Refresh `WORKTREE_SCOPE.md` before the first edit.
1. Check `git status --short --branch` and `git worktree list --porcelain`.
